{% extends 'base.html.twig' %}

{% block title %}Genre index
{% endblock %}

{% block body %}
	<div class="container">
		<div class="d-flex justify-content-between align-items-center mb-4" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
			<h1>🏷️ Genre Index</h1>
			<a href="{{ path('app_genre_new') }}" class="btn btn-primary">
				+ Créer un nouveau
			</a>
		</div>

		<div class="card">
			<div class="table-responsive">
				<table class="table">
					<thead>
						<tr>
							<th>Id</th>
							<th>Nom</th>
							<th>Description</th>
							<th>Actif</th>
							<th>Created At</th>
							<th>Updated At</th>
							<th>Actions</th>
						</tr>
					</thead>
					<tbody>
						{% for genre in genres %}
							<tr>
								<td>{{ genre.id }}</td>
								<td style="font-weight: 600;">{{ genre.nom }}</td>
								<td>{{ genre.description }}</td>
								<td>
									{% if genre.actif %}
										<span class="badge" style="background:#d1fae5; color:#065f46; padding:4px 8px; border-radius:4px; font-size:0.75rem; font-weight:600;">Active</span>
									{% else %}
										<span class="badge" style="background:#fee2e2; color:#991b1b; padding:4px 8px; border-radius:4px; font-size:0.75rem; font-weight:600;">Inactive</span>
									{% endif %}
								</td>
								<td style="font-size: 0.875rem; color: var(--text-muted);">{{ genre.createdAt ? genre.createdAt|date('d/m/Y H:i') : '' }}</td>
								<td style="font-size: 0.875rem; color: var(--text-muted);">{{ genre.updatedAt ? genre.updatedAt|date('d/m/Y H:i') : '' }}</td>
								<td>
									<a href="{{ path('app_genre_show', {'id': genre.id}) }}" class="btn btn-show btn-sm">Voir</a>
									<a href="{{ path('app_genre_edit', {'id': genre.id}) }}" class="btn btn-edit btn-sm">Modifier</a>
								</td>
							</tr>
						{% else %}
							<tr>
								<td colspan="7" style="text-align:center; padding: 2rem; color: #6b7280;">
									No genres found.
									<a href="{{ path('app_genre_new') }}" style="color: var(--primary);">Create one?</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
{% endblock %}
